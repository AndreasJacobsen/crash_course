#In order to include integres in String variables using python the int must be either converted to string before excecuting or converted directly with str(something)
#example

age = 25
message = "Happy " + str(age) + "d birthday"

print(message)
import this;